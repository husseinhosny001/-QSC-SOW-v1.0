usr/bin/env python3
"""سكريبت اختبار للحلقة الرنانة."""

import sys
sys.path.append('../component_library')

from ring_resonator import RingParameters, NonlinearRingResonator, example_ring_design

# تشغيل المثال
print("اختبار تصميم الحلقة الرنانة...")
ring_cell, array_cell = example_ring_design()

print("\n✅ تم اختبار تصميم الحلقة بنجاح!")
print("يمكنك فتح الملفات في KLayout:")
print("  - layouts/gds/nonlinear_ring_resonator.gds")
print("  - layouts/gds/ring_test_array.gds")
EOF

# جعل الملف قابلاً للتنفيذ
chmod +x 03_FABRICATION/scripts/test_ring.py
```

2. تشغيل الاختبار

```bash
# تشغيل اختبار التصميم
cd /QUANTUM_SOVEREIGN_CHIP/03_FABRICATION
python scripts/test_ring.py
```

3. التحقق من المخرجات

بعد التشغيل الناجح، ستكون لديك الملفات التالية:

```
03_FABRICATION/layouts/gds/
├── nonlinear_ring_resonator.gds      # الحلقة الفردية
└── ring_test_array.gds               # مصفوفة الاختبار
```

4. فحص التصميم في KLayout

```bash
# افتح KLayout (إذا كان مثبتاً)
klayout 03_FABRICATION/layouts/gds/nonlinear_ring_resonator.gds
```

📊 خصائص التصميم المنشأة

المكون الوصف الأبعاد المادة
الحلقة الرئيسية حلقة رنانة دائرية نصف قطر 5μm AlGaAs
دلائل الحافلة إدخال/إخراج علوي وسفلي عرض 0.5μm AlGaAs
منطقة الاقتران منطقة الاقتران المحسنة طول 5μm AlGaAs
المسخن الحراري ضبط طور حراري عرض 0.3μm TiN
مقرنات المحزوز إدخال/إخراج ضوئي 8 أسنان AlGaAs
هياكل الاختبار مصفوفة حلقات متنوعة 2×3 مصفوفة متنوع

🔍 الخطوة التالية: التحقق من قواعد التصميم

بعد إنشاء التصميم، يجب التحقق من توافقه مع قواعد التصميم:

```python
# إنشاء سكريبت DRC خاص بالحلقات
cat > 03_FABRICATION/scripts/check_ring_drc.py << 'EOF'
#!/usr/bin/env python3
"""فحص DRC للحلقات الرنانة."""

import gdspy
import yaml

def check_ring_design(gds_file):
    """فحص الحلقات الرنانة مقابل قواعد التصميم."""
    
    # تحميل قواعد التصميم
    with open('../PDK/design_rules.yaml', 'r') as f:
        drc = yaml.safe_load(f)
    
    # تحميل ملف GDS
    lib = gdspy.GdsLibrary(infile=gds_file)
    
    ring_rules = drc['layer_specific'][1]['rules']['ring_resonators']
    
    print("فحص الحلقات الرنانة...")
    print(f"نصف قطر الأدنى: {ring_rules['min_radius']}μm")
    print(f"فجوة الأدنى: {ring_rules['bus_gap_min']}μm")
    
    # هنا يمكن إضافة فحوصات محددة
    
    print("\n✅ فحص DRC مكتمل (مبسط)")
    
if __name__ == "__main__":
    check_ring_design("../layouts/gds/nonlinear_ring_resonator.gds")
EOF
```
